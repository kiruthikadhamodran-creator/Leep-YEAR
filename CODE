import java.io.*
import java.util.*

fun main(args: Array<String>) {

    val year = readLine()!!.toInt()

    if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
        println("True")
    } else {
        println("False")
    }

}
